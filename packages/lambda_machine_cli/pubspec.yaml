name: lambda_machine_cli
description: A sample command-line application with basic argument parsing.
version: 0.0.1
repository: https://github.com/netos23/lambda_machine

environment:
  sdk: ^3.5.2

resolution: workspace

# Add regular dependencies here.
dependencies:
  args: ^2.4.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
